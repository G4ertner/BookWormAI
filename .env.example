# Copy this file to .env.local (or .env when required by the chosen scaffold)
# and add a real key for local development. Never commit the copied file.
# Keep this server-side; do not expose it through an EXPO_PUBLIC_* variable.
OPENAI_API_KEY=
