# Optional local server settings. Copy to .env; never commit real keys.
# Keys saved in narrator settings override these, including explicit removal.
OPENROUTER_API_KEY=
OPENAI_API_KEY=
# Optional Fish voice ID; blank selects the provider default.
FISH_AUDIO_VOICE_ID=
HOST=127.0.0.1
PORT=4310

# Optional local book recommendations. Server-only; independent of narration.
EXA_API_KEY=
